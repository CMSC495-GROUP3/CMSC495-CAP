# The API service. This is what the Dockerfile installs.
-r base.txt

# FastAPI — web framework
fastapi
uvicorn[standard]>=0.31.0
slowapi

# Auth — JWT tokens + password hashing
python-jose[cryptography]
bcrypt
