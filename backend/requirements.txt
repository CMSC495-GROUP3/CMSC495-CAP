# FastAPI — web framework
fastapi
uvicorn[standard]
slowapi

# Auth — JWT tokens + password hashing
python-jose[cryptography]
passlib[bcrypt]
# Pinned: passlib 1.7.4 is incompatible with bcrypt 5.x, which removed the
# __about__ attribute passlib reads to detect the backend version.
bcrypt==5.0.0

# Model provider — used by src/llm.py
openai

# MongoDB — passages, conversations, projects
pymongo

# Environment variable management
python-dotenv
