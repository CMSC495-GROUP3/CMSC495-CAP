# Test and lint dependencies. Install alongside both runtime files:
#   pip install -r requirements.txt -r backend/requirements.txt -r requirements-dev.txt
pytest
pytest-cov
# FastAPI's TestClient is built on httpx
httpx
# Lint and format. Pinned exactly: the formatter's output changes between
# releases, and CI fails on a formatting diff, so everyone must run the same one.
ruff==0.16.5
# Known-vulnerability scan of the Python dependency tree (scripts/audit.sh)
pip-audit
