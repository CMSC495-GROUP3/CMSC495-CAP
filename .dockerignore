# Python
venv/
.venv/
__pycache__/
*.pyc
*.pyo
.env

# Frontend (not needed in the backend build context)
frontend/node_modules/
frontend/dist/
frontend/.vite/

# Sample corpus — uploaded to S3 by seed_documents.py, not needed in the image
data/

# Caddy config is bind-mounted, not baked into an image
Caddyfile

# Git / IDE / docs
.git/
.gitignore
*.md
